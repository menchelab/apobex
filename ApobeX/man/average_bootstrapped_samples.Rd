% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrapped_background_correction.R
\name{average_bootstrapped_samples}
\alias{average_bootstrapped_samples}
\title{Compute Average of Bootstrapped Samples}
\usage{
average_bootstrapped_samples(hash_of_bootstrap_results)
}
\arguments{
\item{hash_of_bootstrap_results}{A hash object where each key contains a list of bootstrapped matrices.}
}
\value{
A hash object where each key contains the average values of the bootstrapped matrices.
}
\description{
This function computes the row-wise average of bootstrapped matrices stored in a hash
and stores the results in a new hash.
}
\details{
This function iterates over each list of bootstrapped matrices in the input hash,
computes the row-wise average for each matrix, and stores the resulting averages in a new hash.
}
\examples{
\dontrun{
library(hash)
bootstrapped_results = hash()
bootstrapped_results[["sample1"]] = matrix(runif(20), nrow = 5)
bootstrapped_results[["sample2"]] = matrix(runif(20), nrow = 5)
average_results = average_bootstrapped_samples(bootstrapped_results)
print(average_results)
}
}
